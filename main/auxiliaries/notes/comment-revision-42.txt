1. В SQL нет оператора &&, надо писать AND
2. В SQL-запросах (заметил я это в последних методах, но, может, и ещё где-то есть) у тебя отстой с балансом скобочек.
// и вообще неплохо запросы проверять в phpMyAdmin :)
3. Для именования полей в базе используется соглашение писать их маленькими буквами через подчеркивания, а для полей и методов объекта используем джавовский стиль: я заметил это в is_approved, может, еще где есть.
4. Про вынесение запросов в ...DBClient я тебе уже сказал. Будь внимателен, к какому клиенту относить запрос (к какой таблице он идёт) и проверяй, не написан ли уже кем-то такой запрос.
5. ЭТО МЕГАВАЖНО!!!! Чтобы обратиться к полю или методу объекта надо обязательно писать $this->tratata или $this->tratata(...) !!!! Без $this-> php будет думать, что ты инициализируешь новую локальную переменную!!!
6. Попрячь методы. Например, loadCompetitions() точно не нужно быть public
7. Ты уверен, что список лиг или турниров не может стать null после загрузки? Мне кажется, что лучше всё-таки завести булевский флаг на это дело...
8. getMostCommonValue нам может понадобиться не только тут, так что я бы предложил сделать её обычной функцией array_most_common_elements() и положил бы в /includes/common.php например, там будем коллекционировать всякие ништяки.)
9. Если возвращаешь ID, то так и пиши getCommonOpponentIds()
10. getStats надо будет потом переделать с учетом методов Game, которые там появятся
11. Комментарии пиши полаконичней.)) В @param и @return указывается только тип данных